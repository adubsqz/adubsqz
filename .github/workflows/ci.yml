name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run lint (TypeScript checks)
        run: npm run lint

      - name: Run unit tests (90% coverage)
        run: npm run test:unit

      - name: Run integration tests (90% coverage)
        run: npm run test:integration

      - name: Install Playwright Chromium
        run: npm run playwright:install

      - name: Run end-to-end tests
        run: npm run test:e2e

      - name: Gallery Python tooling (pytest)
        run: bash tools/run_gallery_pytest.sh -q

      - name: Gallery manifest parity
        run: npm run gallery:verify -- --parity-only

      - name: Run production build
        run: npm run build
